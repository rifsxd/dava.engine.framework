set(MODULE_NAME QtHelpers)

set(MODULE_TYPE STATIC)

set(HPP_FILES_RECURSE *.h *.hpp)
set(CPP_FILES_RECURSE *.cpp *.mm)

set(INCLUDES "${CMAKE_CURRENT_LIST_DIR}" ${DAVA_INCLUDE_DIR})

set(FIND_PACKAGE QT5)

set(QT_UI_FILES "${CMAKE_CURRENT_LIST_DIR}/*.ui")
set(QT_RES_FILES "${CMAKE_CURRENT_LIST_DIR}/*.qrc")

set(INCLUDES_PRIVATE ${CMAKE_CURRENT_BINARY_DIR} ${CMAKE_CURRENT_LIST_DIR})
set(FIND_SYSTEM_LIBRARY_MACOS      Foundation )
set(DEFINITIONS_PRIVATE -D__DAVAENGINE_QT__ )

setup_main_module()
